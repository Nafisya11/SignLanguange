name: frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.10.5+5

  # --- Tambahan ---
  tflite_flutter: ^0.10.4   # biarkan saja, nanti override memaksa versi lain
  permission_handler: ^11.3.0
  image: ^3.3.0
  # ----------------

dependency_overrides:
  tflite_flutter: 0.9.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/alphabet/
    - assets/number/
    - assets/logo/
    - assets/model/
